spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wevi?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      # ???? ???? ????
      # ???? update? ????
      # ?????? ?? ??? ?? ???? ? ??? ?? ?? (??? ???? ??? ?????)
      ddl-auto: create
    properties:
      hibernate:
        # JPA? Hibernate? ???? SQL? ??(System.out?? ??)
        # show_sql: true
        format_sql: true

logging:
  level:
    root: info
    sql: debug
    # JPA? Hibernate? ???? SQL? ??(logger? ??)
    org.hibernate.SQL: debug
#    org.hibernate.type: trace
    org.springframework.security: debug
    com.ssafy.wevi: debug
